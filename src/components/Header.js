import { useState, useRef, useEffect} from "react";

export default function Header(){
    return(
        <div className="bg-green-400 text-white w-full flex justify-between font-semibold h-11 fixed top-0 left-14 z-40 text-xl ">
            Hello, this is a test
        </div>
    )
}